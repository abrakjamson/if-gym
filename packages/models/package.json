{
  "name": "@if-gym/models",
  "version": "0.1.0",
  "description": "Model providers for IF agents",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@if-gym/core": "workspace:*",
    "diff": "^8.0.2",
    "openai": "^6.15.0"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
